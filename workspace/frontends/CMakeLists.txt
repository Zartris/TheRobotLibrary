# frontends — aggregator
#
# The web frontend (frontends/web/) is a TypeScript/Vite application built
# separately via npm. It is NOT added here.
# See frontends/web/README.md for build instructions.

add_subdirectory(native)
